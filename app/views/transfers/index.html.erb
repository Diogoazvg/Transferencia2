<%if current_user.admin?%>
  <h1>Pedidos de Tranferência</h1>

<!--<%= form_tag "/transfers/index" do %>-->
  <!--<radio-button>-->
    <!--<%= radio_button_tag :cond, false, :checked => true%>-->
  <!--</radio-button>-->
<!--<% end %>-->

<table>
  <thead>
    <tr>
      <th>Nome pessoa</th>
      <th>Desc turno &nbsp; &nbsp;</th>
      <th>Periodo atual</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @transfers.each do |transfer| %>
      <tr>
        <td><%= transfer.NOME_PESSOA %>&nbsp; &nbsp;</td>
        <td><%= transfer.DESC_TURNO %></td>
        <td>&nbsp; &nbsp;<%= transfer.PERIODO_ATUAL %></td>
        <td>&nbsp; &nbsp;<%= link_to 'Mostrar pedido', transfer %></td>
        <!--<td><%= link_to 'Mostrar pedido', edit_transfer_path(transfer) %></td>-->
        <!--<td><%= link_to 'Destroy', transfer, method: :delete, data: { confirm: 'Tem certeza?' } %></td>-->
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<!--<%= link_to 'New Transfer', new_transfer_path %>-->
<%else%>
  <p>Área somente para COORDENADOR.</p>

<%end%>
